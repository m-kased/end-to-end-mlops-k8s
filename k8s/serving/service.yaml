apiVersion: v1
kind: Service
metadata:
  name: mlops-serving
  namespace: mlops
  labels:
    app: mlops-serving
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: mlops-serving
